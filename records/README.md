# Records

Each record is a Markdown file with a JSON object between `---` delimiters. JSON is used because it is valid YAML, easy for agents to parse, and requires no special parser for the prototype site. The prose below the metadata preserves editorial context.

This is a provisional format derived from the initial research corpus, not a stable public schema. See [SCHEMA.md](SCHEMA.md).
